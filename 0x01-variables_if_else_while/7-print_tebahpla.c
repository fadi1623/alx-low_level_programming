#include <stdio.h>
/**
 * main - Entry point
 *
 * Descriptiom : A C code that prints from z to a
 *
 * Return: Always 0 (Success)
*/
int main(void)
{
	int i;

	for (i = 122; i > 96; i--)
{
	putchar(i);
}
	putchar('\n');

	return (0);
}
