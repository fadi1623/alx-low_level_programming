#include "main.h"
/**
 * main - Entry point
 *
 * Descriptiom : A C code that checks last digit
 *
 * Return: Always 0 (Success)
*/

int main(void)
void print_alphabet(void);
{
	char c;

	for (c = 'a'; c <= 'z'; c++)
	{
		_putchar(c);
	}

	_putchar('\n');
	return (0);
}
